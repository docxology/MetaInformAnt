# AGENTS.md — `MetaInformAnt/.github/workflows/`

CI workflows (verified 2026-08-29): `test.yml`, `build.yml`, `release.yml`, plus
`README.md`. Changes here affect GitHub Actions runs only — local doc passes
never trigger them.
Repo-wide policy: see the repository-root `AGENTS.md`.