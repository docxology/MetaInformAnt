# AGENTS.md — `MetaInformAnt/.github/`

GitHub configuration for METAINFORMANT (verified 2026-08-29): `workflows/` (CI:
`test.yml`, `build.yml`, `release.yml`, plus a workflows `README.md`).

## Gotchas
- CI runs the repo's no-mocks test policy; do not add mock-based tests.
Repo-wide policy: see the repository-root `AGENTS.md`.